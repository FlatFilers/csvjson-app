function App() {
  return (
    <main className="min-h-svh bg-background text-foreground">
      <h1 className="px-6 pt-8 text-2xl font-semibold tracking-tight">
        CSV to JSON Converter
      </h1>
      <p className="px-6 pt-2 text-sm text-muted-foreground">
        Converter UI lands in the next PR — this scaffold proves the toolchain
        and the conversion service.
      </p>
    </main>
  );
}

export default App;
